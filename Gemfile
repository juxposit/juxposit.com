source "https://rubygems.org"

# If you want to use GitHub Pages, use the github-pages gem
gem "github-pages", group: :jekyll_plugins

group :jekyll_plugins do
  gem "jekyll-feed"
  # Add other supported plugins here, e.g., gem "jekyll-sitemap"
end
